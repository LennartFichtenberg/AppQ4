Der server "app.py" muss gestartet werden mit dem terminal befehl "python app.py" app.py wird auf dem port 8080 laufen. 
Ein ngrok server muss auf den port 8080 verbunden sein.
Dieser ngrok server muss im terminal mit dem befehl "ngrok http 8080" gestartet werden.